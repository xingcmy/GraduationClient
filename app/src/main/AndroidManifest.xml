<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cn.graduationclient">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/xing"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/xing_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.Register"/>
        <activity android:name=".login.Email"/>
        <activity android:name=".homepage.HomePageActivity">
            <intent-filter >
                <action android:name="android.intent.action.VIEW"/>
            </intent-filter>

        </activity>
        <activity android:name=".login.Forget"/>
        <activity android:name=".my.Information"/>
        <activity android:name=".my.AlterInformation"/>
        <activity android:name=".my.friend"/>
        <activity android:name=".my.AddFriend"/>
        <activity android:name=".my.other.Information"/>
        <activity android:name=".message.FriendMessage"/>
        <activity android:name=".login.otherLogin.Email"/>
        <activity android:name=".login.otherLogin.Phone"/>
        <activity android:name=".my.AddFriendInformation"/>
        <activity android:name=".my.AddFriendMsg"/>
        <activity android:name=".message.AgreeRefuse"/>
        <activity android:name=".message.Random.random"/>
        <activity android:name=".message.Random.RandomFriend"/>
    </application>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- 访问internet权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 震动权限 -->
    <uses-permission  android:name="android.permission.VIBRATE"/>

    <!-- 录音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--播音-->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>



    <!-- 可选权限说明-->
    <uses-permission
        android:name="android.permission.INTERNET" />
    <!-- 允许应用程序联网-->
    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--允许访问WiFi网络状态信息-->
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE" />
    <!-- 允许读取手机状态-->
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许访问网络状态-->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许写手机存储-->
    <uses-permission
        android:name="android.permission.RECEIVE_SMS" />
    <!--  GUI自动填充验证码功能-->
    <uses-permission
        android:name="android.permission.READ_SMS" />
    <!--从短信中读取验证码接口-->
    <uses-permission
        android:name="android.permission.READ_CONTACTS" />
    <!--应用内好友功能-->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE" />


</manifest>
